extends layouts/main.pug

block variables
  - const title = 'Meizu'

block content
  include _devices.pug
  include mixins/device.pug
  .wrapper-bg
    include blocks/header.pug
    section.devices
      each device in devices
        +device(device)
    include blocks/footer.pug
